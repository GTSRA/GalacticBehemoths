namespace = aom
# Mind over Matter
country_event = {
	id = aom.1
	title = "aom.1.name"
	desc = "aom.1.desc"
	picture = GFX_evt_psionics
	show_sound = event_mind_over_matter
	is_triggered_only = yes
	trigger = {
		has_tradition = tr_psionics_adopt
	}
	immediate = {
		every_owned_pop = {
			limit = {
				is_same_species = root
				species = {
					has_psionic_species_trait = no
				}
			}
			modify_species = {
				species = this
				add_trait = trait_aom_Belief
			}
			species = {
				save_event_target_as = psionic_species
			}
		}
	}
	option = {
		name = "aom.1.a"
		tooltip = {
			modify_species = {
				species = this
				add_trait = trait_aom_Belief
			}
		}
	}
}
