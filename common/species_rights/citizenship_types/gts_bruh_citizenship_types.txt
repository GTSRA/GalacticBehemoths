citizenship_inside_body_assimilation = {

	# 縮小同化

	pop_group_modifier = {
		pop_happiness = -0.1
		pop_amenities_usage_mult = -0.25
		pop_political_power = -0.5
	}

	potential = {
		OR = {
			has_synthetic_dawn = yes
			has_utopia = yes
			has_machine_age_dlc = yes
			has_biogenesis_dlc = yes
			has_shroud_dlc = yes
		}
		exists = from
		NOT = { has_trait = trait_Micro }
		from = {
			OR = {
				has_origin = origin_Jiju
				has_valid_civic = civic_gts_assimilator
			}
		}
	}

	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			fail_text = MUST_NOT_BE_DOMINANT_SPECIES
			NOT = { is_exact_same_species = from }
		}
		custom_tooltip = {
			fail_text = MUST_NOT_BE_TINY_SPECIES
			gts_is_tiny = yes
		}
	}

	ai_will_do = {
		factor = 100
	}
}

