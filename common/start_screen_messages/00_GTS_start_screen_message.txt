part = {
	location = 0
	localization = "START_SCREEN_MESSAGES_ULTRA_ROAD"
	trigger = {
		#启动标志
		OR = {
			has_country_flag = gts_ultra_road_start_screen
			has_origin = origin_ultra_road
		}
	}
}

part = {
	location = 0
	localization = "START_SCREEN_MESSAGES_NANO"
	trigger = {
		#启动标志
		OR = {
			has_country_flag = gts_nano_start_screen
			has_origin = origin_gts_nano
		}
	}
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_MESSAGES_PARASITIC_ROBOT"	#应该是源代码，留给了汉化
	trigger = {
		#启动标志
		OR = {
			has_country_flag = parasitic_robot_start_screen			#之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_parasitic_robot			#有机器起源
		}
	}
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_MESSAGES_LUANZI"	#应该是源代码，留给了汉化
	trigger = {
		#启动标志
		OR = {
			has_country_flag = Luanzi_start_screen			#之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_Luanzi			#有卵子起源
		}
	}
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_MESSAGES_HEXIE"	#应该是源代码，留给了汉化
	trigger = {
		#启动标志
		OR = {
			has_country_flag = Hexie_start_screen			#之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_Hexie			#有和谐起源
		}
	}
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_MESSAGES_AYA"	#应该是源代码，留给了汉化
	trigger = {
		#启动标志
		OR = {
			has_country_flag = Aya_start_screen			#之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_Aya			#有彩彩起源
		}
	}
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_MESSAGES_JIJU"	#应该是源代码，留给了汉化
	trigger = {
		#启动标志
		OR = {
			has_country_flag = Jiju_start_screen			#之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_Jiju			#有寄居起源
		}
	}
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_MESSAGES_Qianxin"	#应该是源代码，留给了汉化
	trigger = {
		#启动标志
		OR = {
			has_country_flag = Qianxin_start_screen			#之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_Qianxin			#有虔心起源
		}
	}
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_MESSAGES_Fallout"	#应该是源代码，留给了汉化
	trigger = {
		#启动标志
		OR = {
			has_country_flag = Fallout_start_screen			#之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_gts_fallout			#有辐射起源
		}
	}
}

part = {
	location = 0
	localization = "START_SCREEN_GTS_SUBTERRANEAN"
	trigger = {
		has_origin = origin_gts_subterranean
		NOR = {
			is_lithoid_empire = yes
			owner_species = {
				is_species_class = PLANT
			}
			owner_species = {
				is_species_class = FUN
			}
		}
	}
}

part = {
	location = 0
	localization = "START_SCREEN_GTS_SUBTERRANEAN_LITHOID"
	trigger = {
		has_origin = origin_gts_subterranean
		is_lithoid_empire = yes
	}
}

part = {
	location = 0
	localization = "START_SCREEN_GTS_SUBTERRANEAN_PLANT"
	trigger = {
		has_origin = origin_gts_subterranean
		OR = {
			owner_species = {
				is_species_class = PLANT
			}
			owner_species = {
				is_species_class = FUN
			}
		}
	}
}
part = {
	location = 0 #意义不明
	localization = "START_SCREEN_MESSAGES_ANNIHILATOR" #应该是源代码，留给了汉化

	trigger = { #启动标志
			has_country_flag = Annihilator_start_screen #之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_annihilator #有起源
    }
}

part = {
	location = 1 #意义不明
	localization = "START_SCREEN_MESSAGES_ANNIHILATOR_1" #应该是源代码，留给了汉化

	trigger = { #启动标志
			has_country_flag = Annihilator_start_screen #之前的起源给这个国家插了旗子 这里回收了
			has_origin = origin_annihilator #有起源
    }
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_MESSAGES_GOD_KEEPER"	#应该是源代码，留给了汉化
	trigger = {
		has_origin = origin_god_keeper
	}
}

part = {
	location = 0	#意义不明
	localization = "START_SCREEN_ULTRA_COMPANY"	#应该是源代码，留给了汉化
	trigger = {
		has_origin = origin_Ultra_company
	}
}
part = {
	location = 0
	localization = "START_SCREEN_PANLON"
	trigger = {
		has_origin = origin_panlon
	}	
}
part = {
	location = 0 #第一段
	localization = "START_SCREEN_MESSAGES_HSTAR" #应该是源代码，留给了汉化

	trigger = { #启动标志
		has_origin = origin_hstar #有起源
	}
}

part = {
	location = 1 #第一段
	localization = "START_SCREEN_MESSAGES_HSTAR_1"

	trigger = { #启动标志
		has_origin = origin_hstar #有起源
	}
}