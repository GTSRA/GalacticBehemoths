gts_bruh_counter_is_pop = {
	# 計算全部人口(無上限版)
	complex_trigger_modifier = {
		trigger = count_owned_pop_amount
		parameters = {
			limit = {
				pop_has_happiness = yes
			}
		}
		mode = add
		mult = 0.01
	}
}

gts_bruh_counter_is_tiny_pop = {
	# 計算小人人口(無上限版)
	complex_trigger_modifier = {
		trigger = count_owned_pop_amount
		parameters = {
			limit = {
				is_tiny = yes
				pop_has_happiness = yes
			}
		}
		mode = add
		mult = 0.01
	}
}

gts_bruh_counter_is_gts_pop = {
	# 計算巨娘人口(無上限版)
	complex_trigger_modifier = {
		trigger = count_owned_pop_amount
		parameters = {
			limit = {
				is_gts = yes
				pop_has_happiness = yes
			}
		}
		mode = add
		mult = 0.01
	}
}

