#神力提取器
slot_godness_power_government = {
    start = zone_godness_power_default

    included_zone_sets = {
        godness_power_default
    }

    potential = { # planet scope
        always = yes
    }

    unlock = { # planet scope
        always = yes
    }
}
slot_godness_power_empty = { #A dummy zone to prevent building into a district that should not have any.
    include = {
    }

    potential = { # planet scope
        always = no
    }

    unlock = { # planet scope
        always = no
    }
}