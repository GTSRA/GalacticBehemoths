# 计算指定月数的能量币
gts_qhai_energy_count = {
    add_monthly_resource_mult = {
        resource = energy
        value = $COUNT|1$
    }
}

gts_qhai_unity_count = {
    add_monthly_resource_mult = {
        resource = unity
        value = $COUNT|1$
    }
}